#!/bin/sh
set -eu

cd "$(git rev-parse --show-toplevel)"

if ! command -v golangci-lint >/dev/null 2>&1; then
	echo "golangci-lint is required for pre-push. Install: https://golangci-lint.run/welcome/install/" >&2
	exit 1
fi

make check

services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: watchdesk
      MONGO_INITDB_ROOT_PASSWORD: watchdesk
      MONGO_INITDB_DATABASE: watchdesk
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
